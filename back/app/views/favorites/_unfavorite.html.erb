<%= link_to favorite_path(current_user.favorites.find { |b| b.user_id == current_user.id }),
  id: "unfavorite-button-for-movie-#{movie.id}",
  data: { turbo_method: :delete } do %>
  <i class="far fa-heart"></i>
<% end %>